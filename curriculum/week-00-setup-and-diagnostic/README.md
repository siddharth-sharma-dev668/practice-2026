# Week 0 — Setup & Diagnostic (due Sun, Aug 23)

Goal: calibrate the plan to your real level and get your tooling ready. ~2–3 hours total.

## 1. Environment ✅ (mostly done — verified on this machine)

- [x] Python 3.13 — installed
- [x] Git — installed
- [x] Node — installed
- [ ] VS Code with the Python extension (if not already)
- [ ] Run this to confirm pytest works:

```bash
pip install pytest
```

## 2. Accounts

- [ ] **GitHub** — create or clean up. Set a professional username. You'll push everything here.
- [ ] **LeetCode** — free account is enough for now.
- [ ] **LinkedIn** — just make sure it exists and matches your resume; we polish it at Week 8.

## 3. The Diagnostic (the important part)

Open the **platform** (`platform/index.html` — double-click it, or use the hosted link Claude gave you) → **Quizzes** → **Diagnostic**.

- 25 questions across 6 sections: Python, SQL, DSA, Web/Backend, Stats, ML/AI
- Closed book, no googling — it measures where you are, not what you can look up
- It shows per-section scores at the end

## 4. Coding warm-up (15 min, honest timing)

In this folder, create `warmup.py` and implement — without looking anything up:

```python
def most_common_word(text: str) -> str:
    """Return the most frequent word (lowercase, ignore punctuation).
    Ties: return the alphabetically first. most_common_word("The cat and the dog") -> "the"
    """
```

Note how long it took you and whether it ran correctly first try.

## 5. Send me (in chat) — this unlocks Week 1

1. Diagnostic **section scores** (e.g., "Python 5/6, SQL 2/4, ...")
2. Your **hours/week** you can truly commit, and any deadline (visa, appraisal cycle, target company)
3. Your **GitHub username**
4. How the **warm-up** went (time + did it work)

I'll recalibrate the roadmap (skip/compress weeks you ace, reinforce weak sections) and generate anything Week 1 is missing for your level.
